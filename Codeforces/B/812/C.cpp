#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int n, a[N]

void solve(){
    cin >> n;
    int s = sqrt(n) + 1;
    s *= s;

    int l = n - 1, n - 1;
    for(; l >= 0;){
        
    }
}

int main(){
    int T;
    cin >> T;
    for(;T--;) solve();
    return 0;
}